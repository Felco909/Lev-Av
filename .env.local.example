# Local override template.
# Copy this file to .env.local and set local-safe values.

NEXTAUTH_SECRET=replace_with_local_secret
NEXTAUTH_URL=http://localhost:3000

# IMPORTANT: use non-production DB locally
DATABASE_URL=postgresql://user:password@localhost:5432/levav_tms_local

AWS_REGION=us-west-2
AWS_BUCKET_NAME=local_or_dev_bucket
AWS_FOLDER_PREFIX=local/

ABACUSAI_API_KEY=replace_with_local_token
