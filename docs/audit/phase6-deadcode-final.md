# TMS Audit — Фаза 6: Dead code / дублирование логики

> **Обновление 2026-08-09:** TMS-AUDIT-0044, 0045, 0046, 0047 исправлены. 0044 — карты
> подписей статуса сведены к `lib/trip-workflow-filters.ts:TRIP_STATUS_LABELS_RU`; заодно
> найден и сведён 5-й дубль (карта статуса оплаты, задвоенная в `trips/page.tsx`). 0045 — из
> списка кандидатов удалены 6 подтверждённо неиспользуемых роутов (каждый проверен точечным
> grep перед удалением: `reports/company-debts`, `company-base/status`, `company-base/check`,
> `wialon/sync-mileage`, `clients/[id]/document-number-warnings`, `clients/[id]/next-doc-pair`);
> `/api/health` **не удалён** — используется `hooks/use-server-sync.ts`. 0046 — модель
> `Maintenance` удалена (миграция `20260809163519_remove_maintenance_model`) вместе с 4
> местами чтения (не 3, как предполагалось изначально — обнаружено при реализации), включая
> `lib/finance/vehicle-maintenance-expenses.ts` (часть фикса 0023 из фазы 3, всегда суммировала
> 0 из этой модели — функционально ничего не изменилось). 0047 — 33 неиспользуемых прод-/
> dev-зависимости удалены после точечной проверки каждой (`depcheck` + grep по `app/`/`lib/`/
> `scripts/`/конфигам — часть кандидатов депчека оказалась ложным срабатыванием: `dotenv`,
> `autoprefixer`, `postcss`, ESLint-плагины остались, т.к. реально используются через
> CLI-флаги/конфиги, не прямой импорт). `npm run build` и `npm test` — зелёные.

Дата: 2026-08-09
Метод: статический аудит (1 исследование). Хорошая новость: кодовая база чистая — **ноль** TODO/FIXME/HACK, **ноль** `console.log`-отладки, **ноль** `if(true)/if(false)`-заглушек, **ноль** mock/dummy-данных в `app/`/`lib/`. Единственный закомментированный блок (`app/api/agents/document/route.ts:41-71`) — осознанно оставленная предыдущая реализация на Claude/Anthropic после перехода на Gemini, помечена как "для отката".

---

## 🟡 MEDIUM

### TMS-AUDIT-0044 — 4 независимых карты русских подписей статуса заявки, уже разошедшиеся по формулировкам
**Раздел:** UI / Дублирование логики
**Проблема:** Помимо уже известной пары `STATUS_MAP` (`lib/utils.ts`) и `TRIP_STATUS_LABELS_RU` (`lib/trip-workflow-filters.ts`, находка фазы 2), найдены ещё 2: `app/api/reports/overview/route.ts:122` и `app/(app)/trips/_components/trip-form.tsx:1016` — все 4 уже разошлись в формулировках. Плюс карта статусов оплаты продублирована дважды внутри одного файла `app/(app)/trips/page.tsx`.
**Рекомендация:** Свести к одному экспортируемому источнику (например, расширить `STATUS_MAP` в `lib/utils.ts` до единственной точки истины), остальные 3+1 переиспользовать.

---

## 🔵 LOW

### TMS-AUDIT-0045 — Кандидаты на удаление: мёртвые/замещённые эндпоинты и функции
Помимо уже отмеченных в фазах 1-2 (`assertCompletedWorkflowTransition`, `assertReopenToAwaitingPaymentTransition`, `POST /api/trips/[id]/close`, `handleCompleteTrip`, `app/api/auth/login`, `/api/reports/trips?format=csv`, диагностические функции `lib/vehicle-trips/revenue.ts`), подтверждены как без вызывающей стороны во фронтенде:
- `app/api/reports/company-debts` — подтверждено комментарием в самом `lib/finance/debts-service.ts`: заменён на `/api/debts`.
- `app/api/documents/generate` (295 строк) — по факту заменён на `trips/[id]/generate-docs`. **Важно:** фаза 4 нашла, что теоретически он всё ещё вызывается из `app/api/agents/document/route.ts:429` (AI-агентский флоу) — прежде чем удалять, надо явно проверить, действительно ли этот путь агента используется, иначе удаление сломает функциональность агента. Если подтвердится, что мёртв полностью — заодно закрывает TMS-AUDIT-0007 (потерянный НДС в этом эндпоинте) простым удалением, а не фиксом.
- `clients/[id]/document-number-warnings`, `clients/[id]/next-doc-pair`, `company-base/status`, `company-base/check`, `api/wialon/sync-mileage` (сама функция синхронизации вызывается из `scripts/wialon-sync-mileage.ts` напрямую, минуя HTTP-роут — сам роут, похоже, для ручного триггера, которого нет в UI), `api/health`.
**Рекомендация:** Перед удалением каждого — точечно подтвердить (grep + ручная проверка), что нет вызовов из scripts/cron/внешних интеграций. Не удалять массово одним коммитом.

### TMS-AUDIT-0046 — Prisma-модель `Maintenance` не имеет ни одного пути записи — таблица всегда пуста
**Раздел:** БД / Собственный транспорт
**Проблема:** `Maintenance` читается в 3 местах, но нигде не создаётся (ни в UI, ни в API, ни в сидах). Скорее всего вытеснена моделью `ServiceRecord`/`ServiceRegulation` (используются штатно, подтверждено в фазе 3), но не удалена из схемы.
**Рекомендация:** Подтвердить, что не используется внешним инструментом напрямую через БД, затем удалить модель в отдельной Prisma-миграции.

### TMS-AUDIT-0047 — ~15 неиспользуемых npm-зависимостей
**Раздел:** Зависимости
**Проблема:** `react-hook-form`, `zod`, `@hookform/resolvers`, `mapbox-gl`, `plotly.js`/`react-plotly.js`, `chart.js`/`react-chartjs-2`, `formik`, `yup`, `jotai`, `zustand`, `jsonwebtoken`, `gray-matter`, `lodash`, `file-saver`, `react-is`, `react-datepicker`, `dayjs`, `@azure/storage-blob` — не импортируются нигде в `app/`/`lib/` (точечная проверка, не исчерпывающая).
**Рекомендация:** Прогнать `depcheck` или аналог для полного списка, удалить пакетами, не одним махом — часть могла остаться от раннего прототипа других страниц.

---

## Что уже подтверждено чистым
- Ноль TODO/FIXME/HACK/XXX во всём `app/`/`lib/`.
- Ноль debug `console.log`, ноль `if(true)/if(false)`-заглушек, ноль mock/dummy-данных в продакшен-коде.
- ~90 из ~99 API-роутов имеют подтверждённого вызывающего во фронтенде или в scripts/cron.
- `validateTripPayload` (серверная валидация) синхронизирована с клиентской формой заявки — не найдено расхождений (подтверждает находку фазы 2).
- 28 из 29 Prisma-моделей имеют живые чтение+запись.
